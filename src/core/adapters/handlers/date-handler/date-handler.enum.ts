export enum DATE_CHUNK {
  MILLISECONDS = 'milliseconds',
  SECONDS = 'seconds',
  MINUTES = 'minutes',
  HOURS = 'hours',
  DAYS = 'days',
  WEEKS = 'weeks',
  MONTHS = 'months',
  YEARS = 'years',
}

export enum DATE_FORMAT {
  BRAZILIAN_DATE = 'dd/MM/yyyy',
  TIMESTAMP_IN_SECONDS = 'TimestampInSeconds',
  ISO = 'ISO',
  DASHED_DATE = 'yyyy-MM-dd',
  SLASHED_DATE = 'yyyy/MM/dd',
  BRAZILIAN_STANDARD = 'dd/MM/yyyy HH:mm',
  STANDARD = 'yyyy-MM-dd HH:mm:ss',
  DATETIME = 'dateTime',
  DATE = 'date',
}
